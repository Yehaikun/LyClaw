<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- ========== 父 POM 不再继承 spring-boot ========== -->
    <!-- 只有最终的启动模块才继承 spring-boot-starter-parent -->

    <groupId>lyjew.com</groupId>
    <artifactId>LyClaw</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>pom</packaging>  <!-- ← 关键：父工程打包方式为 pom -->
    <name>LyClaw</name>
    <description>LyClaw Multi-Module</description>

    <!-- ========== 声明子模块 ========== -->
    <modules>
        <module>lyclaw-common</module>
        <module>lyclaw-core</module>
        <module>lyclaw-storage</module>
        <module>lyclaw-adapter</module>
        <module>lyclaw-web</module>
        <module>lyclaw-engine</module>
    </modules>

    <!-- ========== properties 统一定义版本 ========== -->
    <properties>
        <java.version>17</java.version>
        <maven.compiler.source>17</maven.compiler.source>
        <maven.compiler.target>17</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- 依赖版本统一管理 -->
        <spring-boot.version>3.5.14</spring-boot.version>
        <okhttp.version>4.12.0</okhttp.version>
        <retrofit.version>2.9.0</retrofit.version>
        <hutool.version>5.8.44</hutool.version>
        <reactor.version>3.6.8</reactor.version>
        <lombok.version>1.18.36</lombok.version>
    </properties>

    <!-- ========== dependencyManagement 统一管理依赖 ========== -->
    <!-- 这里只声明版本，不实际引入 -->
    <dependencyManagement>
        <dependencies>
            <!-- Spring Boot BOM（统一管理所有 spring-boot 依赖版本） -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- 自己的模块 -->
            <dependency>
                <groupId>lyjew.com</groupId>
                <artifactId>lyclaw-common</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>lyjew.com</groupId>
                <artifactId>lyclaw-storage</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>lyjew.com</groupId>
                <artifactId>lyclaw-adapter</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- 加 core -->
            <dependency>
                <groupId>lyjew.com</groupId>
                <artifactId>lyclaw-core</artifactId>
                <version>${project.version}</version>
            </dependency>

            <dependency>
                <groupId>lyjew.com</groupId>
                <artifactId>lyclaw-engine</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- 第三方依赖版本锁定 -->
            <dependency>
                <groupId>com.squareup.okhttp3</groupId>
                <artifactId>okhttp</artifactId>
                <version>${okhttp.version}</version>
            </dependency>
            <dependency>
                <groupId>com.squareup.okhttp3</groupId>
                <artifactId>logging-interceptor</artifactId>
                <version>${okhttp.version}</version>
            </dependency>
            <dependency>
                <groupId>com.squareup.retrofit2</groupId>
                <artifactId>retrofit</artifactId>
                <version>${retrofit.version}</version>
            </dependency>
            <dependency>
                <groupId>cn.hutool</groupId>
                <artifactId>hutool-all</artifactId>
                <version>${hutool.version}</version>
            </dependency>
            <dependency>
                <groupId>io.projectreactor</groupId>
                <artifactId>reactor-core</artifactId>
                <version>${reactor.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <!-- ========== 公共依赖（所有子模块都会继承） ========== -->
    <dependencies>
        <!-- Lombok（所有模块都需要） -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- 测试（所有模块都需要） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- 编译插件 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <source>17</source>
                    <target>17</target>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>   <!-- ← 补上这行 -->
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>