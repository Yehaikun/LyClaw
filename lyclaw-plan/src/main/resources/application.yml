spring:
  application:
    name: lyclaw-plan-service
server:
  port: 8083
management:
  endpoints:
    web:
      exposure:
        include: health,metrics
  endpoint:
    health:
      show-details: always
