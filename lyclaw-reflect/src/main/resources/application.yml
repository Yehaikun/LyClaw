spring:
  application:
    name: lyclaw-reflect-service
server:
  port: 8085
management:
  endpoints:
    web:
      exposure:
        include: health,metrics
  endpoint:
    health:
      show-details: always
