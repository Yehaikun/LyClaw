spring:
  application:
    name: lyclaw-memory-service
server:
  port: 8082
management:
  endpoints:
    web:
      exposure:
        include: health,metrics
  endpoint:
    health:
      show-details: always
